import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#833433",
      light: "#c89c8a",
      dark: "#5f191c",
    },
  },
});

export default theme;
